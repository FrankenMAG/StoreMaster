@model IEnumerable<StoreMaster.Web.ViewModels.CategoriaViewModel>
@{
    ViewData["Title"] = "Categorías";
}

<!-- Encabezado -->
<div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="mb-0 fw-bold">
        <i class="fas fa-tags me-2 text-primary"></i>Categorías
    </h4>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus me-2"></i>Nueva Categoría
    </a>
</div>

<!-- Tabla -->
<div class="card">
    <div class="card-body p-0">
        <table class="table table-hover mb-0" id="tablaCategorias">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Descripción</th>
                    <th>Estado</th>
                    <th>Creada</th>
                    <th class="text-center">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var categoria in Model)
                {
                    <tr>
                        <td class="fw-semibold">@categoria.Nombre</td>
                        <td class="text-muted">@(categoria.Descripcion ?? "—")</td>
                        <td>
                            @if (categoria.Activa)
                            {
                                <span class="badge bg-success">Activa</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">Inactiva</span>
                            }
                        </td>
                        <td class="text-muted small">
                            @categoria.CreadoEn.ToString("dd/MM/yyyy")
                        </td>
                        <td class="text-center">
                            <a asp-action="Edit" asp-route-id="@categoria.Id"
                               class="btn btn-sm btn-outline-primary me-1">
                                <i class="fas fa-edit"></i>
                            </a>
                            @if (categoria.Id != 1)
                            {
                                <form asp-action="Delete" asp-route-id="@categoria.Id"
                                      method="post" class="d-inline"
                                      onsubmit="return confirm('¿Eliminar categoría @categoria.Nombre?')">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-outline-danger">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </form>
                            }
                        </td>
                    </tr>
                }

            </tbody>
        </table>
    </div>
</div>
@section Scripts {
    <script>
        $(document).ready(function () {
            $('#tablaCategorias').DataTable({
                language: {
                    url: 'https://cdn.datatables.net/plug-ins/1.13.7/i18n/es-MX.json',
                    emptyTable: 'No hay categorias registradas'
                },
                pageLength: 10,
                order: [[0, 'asc']],
                columnDefs: [
                    { orderable: false, targets: -1 }
                ]
            });
        });
    </script>
}